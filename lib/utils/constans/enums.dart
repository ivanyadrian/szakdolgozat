/// List of enums
/// They cannot be created inside a class
library;

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered } /// ezt valószínűleg nem fogom használni

enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm } ///ezt valószínűleg nem fogom használni