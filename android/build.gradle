buildscript {
    repositories {
        google()  // Győződj meg róla, hogy ez itt van
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.4' // Ellenőrizd, hogy az eszközkészleted verziója megfelelő
        classpath 'com.google.gms:google-services:4.4.2' // Ez a plugin verziója lehet változó
    }
}

allprojects {
    repositories {
        google()  // Győződj meg róla, hogy ez itt van
        mavenCentral()
    }
}


allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}